def add(a: float, b: float) -> float:
    """Returns the result of `a` added with `b`"""
    return a + b

def multiply(a: float, b: float) -> float:
    """Returns the result of `a` multiplied with `b`"""
    return a * b
